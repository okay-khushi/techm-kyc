[pytest]
# Run from the backend/ directory: `python -m pytest`
# pythonpath = . puts backend/ on sys.path so `import app...` resolves without
# an installed package. No network, DB, or external services are required.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
